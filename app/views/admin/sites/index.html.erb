<div class="admin">
  <div class="clearfix">
    <h3 class="pull-left">Sites</h3>
    <div class="pull-right header-button"><%= link_to 'Add Site', new_admin_site_path, class: 'btn btn-school'%></div>  
  </div>
  <p class="description">The site section allows the administrator to add different local sites/locations to the system.  These sites can then be tagged on a particular trial.</p>
  
  <table class="table table-bordered table-trials">
    <tr>
      <th>Site Name</th>
      <th>Address</th>
      <th>City</th>
      <th>State</th>
      <th>Zip</th>
      <th></th>
    </tr>
    <% if @sites.empty? %>
      <tr>
        <td colspan="6">There are no sites loaded.  Add sites manually using the add button above.</td>
      </tr>
    <% end %>
    <% @sites.each do |s| %>
      <tr>
        <td><%= s.site_name %></td>
        <td><%= s.address %></td>
        <td><%= s.city %></td>
        <td><%= s.state %></td>
        <td><%= s.zip %></td>
        <td>
          <%= link_to 'Edit', edit_admin_site_path(s.id), class: 'btn btn-primary' %>
          <%= link_to 'Delete', admin_site_path(s.id), method: 'delete', class: 'btn btn-danger', data: { confirm:"Are you sure you want to delete this site?" }%>
        </td>
      </tr>
    <% end %>
  </table>
</div>