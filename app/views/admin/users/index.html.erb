<div class="admin">
  <div class="clearfix">
    <h3 class="pull-left">Users</h3>
    <div class="pull-right header-button"><%= link_to 'Add User', new_admin_user_path, class: 'btn btn-school'%></div>  
  </div>
  <p class="description">This section is where the administrator can add/edit other admin users within StudyFinder.  To add a new user click the "Add User" button above.  You will need their unique email and internet_id.  Once a user has been added, they will be able to access the entire admininstration area, so only grant these priveledges to administrative staff.</p>
  
  <table class="table table-bordered table-trials">
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th></th>
    </tr>
    <% if @users.empty? %>
      <tr>
        <td colspan="3">There are no users loaded. Add users manually using the add button above.</td>
      </tr>
    <% end %>
    <% @users.each do |u| %>
      <tr>
        <td><%= u.full_name %></td>
        <td><%= mail_to u.email%></td>
        <td style="width: 150px">
          <%= link_to 'Edit', edit_admin_user_path(u.id), class: 'btn btn-primary' %>
          <%= link_to 'Delete', admin_user_path(u.id), method: 'delete', class: 'btn btn-danger', data: { confirm:"Are you sure you want to delete this user?" }%>
        </td>
      </tr>
    <% end %>
  </table>
</div>