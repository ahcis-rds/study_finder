<!DOCTYPE html>
<html class="no-js" lang="en">
<head>
  <%= render 'shared/analytics4' if SystemInfo.ga_version == 4 %>
  <title><%= @system_info.initials %> - <%= @system_info.system_name %></title>
  <link href="https://fonts.googleapis.com/css?family=Raleway:200,300,400,500,700" type="text/css" rel="stylesheet" media="all" />
  <link href="https://fonts.googleapis.com/css?family=Prompt:400,700" type="text/css" rel="stylesheet" />
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag 'application', media: 'all', rel: 'stylesheet' %>
  <%= javascript_include_tag 'application' %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <link rel="apple-touch-icon" href="assets/logo180.png">
  <%= yield :head %>
</head>
<body>
  <script>
    const DEFAULT_EMAIL = '<%= SystemInfo.default_email %>';
    const STUDY_CONTACT_BCC = '<%= SystemInfo.study_contact_bcc %>';
    const GA_VERSION = <%= SystemInfo.ga_version %>;
  </script>
  <div class="container main-content">
    <%= render 'shared/navbar' %>
    <%= render 'shared/flash', flash: flash %>
    <%= render 'shared/breadcrumb' %>
    <%= yield %>
    <hr>
    <%= render 'shared/footer' %>
  </div>
  <%= render 'shared/analytics3' if SystemInfo.ga_version == 3 %>
</body>
</html>
