local:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>
 
test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

staging:
  service: Disk
  root: <%= Rails.root.join("storage") %>

production:
  service: Disk
  root: <%= Rails.root.join("storage") %>